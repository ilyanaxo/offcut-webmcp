Offcut gives people and browser agents the same cutting-stock workbench. Fourteen parts fit; the long board stays protected.

These illustrative measurements cover usable stock, parts, kerf and the reusable-remnant threshold. People own measurements and protection; registered tools cannot change them.

The right-hand panel is an external model recording harness, not a chatbot inside the website. A real model chooses native Web M C P calls on the shared committed workbench. This is not a ChatGPT recording.

The model compares three complete alternatives: less stock, fewer boards, or useful offcuts. The deterministic branch-and-bound solver checks quantities, stock and one kerf per part. A search limit is not a proof of optimality.

Less stock and fewer boards both open seven point two metres across three boards: forty-two millimetres of sawdust, plus one hundred fifty-eight millimetres of short scrap. Keeping useful offcuts opens ten point zero eight metres across six boards: forty-two millimetres of sawdust, no short scrap, and three point zero three eight metres reusable. Computed trade-offs, not measured time or carbon savings.

The agent inspects and stages the exact plan. Pause for review: this is not approval. Check stock identities, quantities and kerf. No registered tool can approve. These UI controls are automated, not proof of human presence. Now the controls check the acknowledgement. Then they click Approve cut sheet.

The model inspects approval and exports the actual C S V: fourteen parts, part identifiers, offsets, kerf, and the exact approved plan identifier. Another draft cannot inherit approval.

Typing four leaves committed kerf at three. Pending edits block staging without moving focus or saving. Now commit: revision two invalidates the old approval. The old plan is stale. The model replans: fourteen parts, fifty-six millimetres of sawdust. Revision two is staged, not approved. The acknowledgement stays unchecked.

Offcut itself needs no account, model key or backend. The workbench and M I T licensed source are public. These are planning estimates, not machine instructions: verify usable stock and safe cutting conditions at the bench.
